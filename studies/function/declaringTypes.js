console.log(sum(3, 4))
//console.log(multi(3, 4)) //not initializated

// function declaration
function sum(x, y) {
    return x + y
}
    
//functuon expression
const sub = function (x, y) {
    return x - y
}

// named function expression (less used in JS)
    const mult = function mult(x, y) {
    return x * y
}