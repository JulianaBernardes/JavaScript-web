function expression (dividend, divader) {
    console.log(`The result of the division between ${dividend} and ${divader} is`, dividend/divader)
    console.log(`The rest of the division between ${dividend} and ${divader} is`, dividend % divader)
}
expression(4,3)